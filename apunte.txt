webpack : paquete a investigar , principalmente crea compatibilidad con navegadores viejos evitar almacenamiento en cache de terceros

-node modules lo crea la instalacion del webpack , webpack cli 
-packagejson - script : { "build" : ejecuta webpack, crea carpeta dist con archivo main.js } - ejecucion manual
la carpeta dist contiene los archivos procesados, para subir a produccion.

-carpeta : 13-08 continuar con video 7 archivo de configuracion del webpack.

*instalaciones de node:
-webpack , webpackcli
-server
-npm i -D html-loaded html-webpack-plugin (el primero hace que el index.html se incluya en carpeta /dist, y el segundo hace que el main se incluya en el html )
-npm i -D css-loader style-loader (para leer el archivo minimizarlo y llevarlo al bundler, o tambien llamado 'main.js')
npm install --save-dev mini-css-extract-plugin (dejar el archivo styles en la carpeta dis , como un archivo independiente)

*webpack.config:(ver documentacion actualizada)

-test: /\.html$/ (aplicar la regla si es un archivo html, imdicamos que debe mirar y em use: que debe aplicar)

*styles: (css global)